.panel.panel-primary
  .panel-heading.text-center New Courses
  .pane-body
    .row(ng-repeat="course in courses | orderBy:'published':true | limitTo:10")
      .col-md-3 {{ course.published | date:'y- MMM d' }}
      .col-md-9 
        a(href="/courses/{{course._id}}") {{course.title}}
